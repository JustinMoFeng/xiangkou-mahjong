# Pin the public npm registry for this project so package-lock.json never gets
# rewritten with an internal mirror (e.g. bnpm.byted.org) when installing on a
# corp network. GitHub-hosted CI runners can only reach the public registry.
registry=https://registry.npmjs.org/
